/**
 * 4.1. Создать программу вычисляющую формулу квадратного уравнения
 *
 * 1. Создать класс Square в нем один методы float calculate(int x) и void show(start, finish, step).
 * Метод должен вычислять значение функции y = a*x2 + b * x + c; параметры a b c задаются через конструктор.
 * void show(start, finish, step) - вывести на экран значение функции в диапазоне от x1 до x2 c шагом step.
 * 2. Обязательно нужно добавить тесты проверяющие методы.
 */
package andrusyak.task_01;